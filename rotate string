problem - rotate string
class Solution {
    public boolean rotateString(String s, String goal) {
        if(s.length()!= goal.length())
        return false;

        String u = s + s;
        return u.contains(goal);

        
    }
}
